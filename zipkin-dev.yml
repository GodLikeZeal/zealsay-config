server:
  port: 8072

zipkin:
  collector:
    rabbitmq:
      addresses: localhost:5682
      password: lengleng
      username: pig
      queue: zipkin
  storage:
    type: elasticsearch
    elasticsearch:
      hosts: localhost:9200
      cluster: elasticsearch
      index: zipkin
      max-requests: 64
      index-shards: 5
      index-replicas: 1
